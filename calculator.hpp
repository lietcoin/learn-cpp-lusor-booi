#ifndef INDEX_CALCULATOR
#define INDEX_CALCULATOR

double getNumber();
char getSymbol();
double Calculations(double first, double second, char symbol);
void PrintResults(double first, double second, char symbol);

#endif